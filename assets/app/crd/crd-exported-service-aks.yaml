apiVersion: consul.hashicorp.com/v1alpha1
kind: ExportedServices
metadata:
  name: aks
spec:
  services:
    - name: product-api
      namespace: default
      consumers:
        - partition: default
